import { ConvertToMorsePipe } from './convert-to-morse.pipe';

describe('ConvertToMorsePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToMorsePipe();
    expect(pipe).toBeTruthy();
  });
});
