import { ConvertToHashPipe } from './convert-to-hash.pipe';

describe('ConvertToHashPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToHashPipe();
    expect(pipe).toBeTruthy();
  });
});
